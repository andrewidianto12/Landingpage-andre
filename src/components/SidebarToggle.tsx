'use client';import React from 'react';export default function SidebarToggle({ className = '' }: { className?: string }) {  const open = () => {    if (typeof window !== 'undefined') {      window.dispatchEvent(new CustomEvent('open-sidebar'));    }  };  return (    <button      onClick={open}      className={`inline-flex items-center gap-2 px-2 py-1 rounded-md text-sm text-gray-200 hover:text-white transition ${className}`}      aria-label="Open menu"      title="Open menu"    >      ☰    </button>  );}